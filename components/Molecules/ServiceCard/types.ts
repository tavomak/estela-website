export interface ServiceCardProps {
  title: string;
  containerClass?: string;
  description: string | string[];
  bgColor: string;
  accentColor: string;
  textColor?: string;
  buttonText?: string;
}
