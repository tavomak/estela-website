export interface Testimonial {
  projectName: string;
  testimonialText: string;
  highlightedQuote?: string;
  image: { url: string };
  autorName: string;
  autorRole: string;
  bgColor: string;
  accentColor: string;
}
